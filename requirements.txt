# Service
fastapi>=0.110
uvicorn>=0.29

# Development UI
streamlit>=1.30,<2.0

# Optional: improve parser accuracy. The pipeline runs without them.
tldextract>=5.0
python-magic>=0.4.27    # requires libmagic: sudo apt install -y libmagic1

# The evaluation/ scripts use the standard library only.
aiosmtpd>=1.4
